// Code generated by entc, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"

	"github.com/OMENX/app/ent/discipline"
	"github.com/OMENX/app/ent/user"
	"github.com/facebookincubator/ent/dialect/sql/sqlgraph"
	"github.com/facebookincubator/ent/schema/field"
)

// DisciplineCreate is the builder for creating a Discipline entity.
type DisciplineCreate struct {
	config
	mutation *DisciplineMutation
	hooks    []Hook
}

// SetDiscipline sets the discipline field.
func (dc *DisciplineCreate) SetDiscipline(s string) *DisciplineCreate {
	dc.mutation.SetDiscipline(s)
	return dc
}

// AddUserIDs adds the users edge to User by ids.
func (dc *DisciplineCreate) AddUserIDs(ids ...int) *DisciplineCreate {
	dc.mutation.AddUserIDs(ids...)
	return dc
}

// AddUsers adds the users edges to User.
func (dc *DisciplineCreate) AddUsers(u ...*User) *DisciplineCreate {
	ids := make([]int, len(u))
	for i := range u {
		ids[i] = u[i].ID
	}
	return dc.AddUserIDs(ids...)
}

// Mutation returns the DisciplineMutation object of the builder.
func (dc *DisciplineCreate) Mutation() *DisciplineMutation {
	return dc.mutation
}

// Save creates the Discipline in the database.
func (dc *DisciplineCreate) Save(ctx context.Context) (*Discipline, error) {
	if _, ok := dc.mutation.Discipline(); !ok {
		return nil, &ValidationError{Name: "discipline", err: errors.New("ent: missing required field \"discipline\"")}
	}
	var (
		err  error
		node *Discipline
	)
	if len(dc.hooks) == 0 {
		node, err = dc.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*DisciplineMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			dc.mutation = mutation
			node, err = dc.sqlSave(ctx)
			mutation.done = true
			return node, err
		})
		for i := len(dc.hooks) - 1; i >= 0; i-- {
			mut = dc.hooks[i](mut)
		}
		if _, err := mut.Mutate(ctx, dc.mutation); err != nil {
			return nil, err
		}
	}
	return node, err
}

// SaveX calls Save and panics if Save returns an error.
func (dc *DisciplineCreate) SaveX(ctx context.Context) *Discipline {
	v, err := dc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

func (dc *DisciplineCreate) sqlSave(ctx context.Context) (*Discipline, error) {
	d, _spec := dc.createSpec()
	if err := sqlgraph.CreateNode(ctx, dc.driver, _spec); err != nil {
		if cerr, ok := isSQLConstraintError(err); ok {
			err = cerr
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	d.ID = int(id)
	return d, nil
}

func (dc *DisciplineCreate) createSpec() (*Discipline, *sqlgraph.CreateSpec) {
	var (
		d     = &Discipline{config: dc.config}
		_spec = &sqlgraph.CreateSpec{
			Table: discipline.Table,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: discipline.FieldID,
			},
		}
	)
	if value, ok := dc.mutation.Discipline(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: discipline.FieldDiscipline,
		})
		d.Discipline = value
	}
	if nodes := dc.mutation.UsersIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.O2M,
			Inverse: false,
			Table:   discipline.UsersTable,
			Columns: []string{discipline.UsersColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeInt,
					Column: user.FieldID,
				},
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_spec.Edges = append(_spec.Edges, edge)
	}
	return d, _spec
}
