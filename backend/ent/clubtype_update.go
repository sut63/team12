// Code generated by entc, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"

	"github.com/OMENX/app/ent/club"
	"github.com/OMENX/app/ent/clubtype"
	"github.com/OMENX/app/ent/predicate"
	"github.com/facebookincubator/ent/dialect/sql"
	"github.com/facebookincubator/ent/dialect/sql/sqlgraph"
	"github.com/facebookincubator/ent/schema/field"
)

// ClubTypeUpdate is the builder for updating ClubType entities.
type ClubTypeUpdate struct {
	config
	hooks      []Hook
	mutation   *ClubTypeMutation
	predicates []predicate.ClubType
}

// Where adds a new predicate for the builder.
func (ctu *ClubTypeUpdate) Where(ps ...predicate.ClubType) *ClubTypeUpdate {
	ctu.predicates = append(ctu.predicates, ps...)
	return ctu
}

// SetName sets the name field.
func (ctu *ClubTypeUpdate) SetName(s string) *ClubTypeUpdate {
	ctu.mutation.SetName(s)
	return ctu
}

// AddClubIDs adds the club edge to Club by ids.
func (ctu *ClubTypeUpdate) AddClubIDs(ids ...int) *ClubTypeUpdate {
	ctu.mutation.AddClubIDs(ids...)
	return ctu
}

// AddClub adds the club edges to Club.
func (ctu *ClubTypeUpdate) AddClub(c ...*Club) *ClubTypeUpdate {
	ids := make([]int, len(c))
	for i := range c {
		ids[i] = c[i].ID
	}
	return ctu.AddClubIDs(ids...)
}

// Mutation returns the ClubTypeMutation object of the builder.
func (ctu *ClubTypeUpdate) Mutation() *ClubTypeMutation {
	return ctu.mutation
}

// RemoveClubIDs removes the club edge to Club by ids.
func (ctu *ClubTypeUpdate) RemoveClubIDs(ids ...int) *ClubTypeUpdate {
	ctu.mutation.RemoveClubIDs(ids...)
	return ctu
}

// RemoveClub removes club edges to Club.
func (ctu *ClubTypeUpdate) RemoveClub(c ...*Club) *ClubTypeUpdate {
	ids := make([]int, len(c))
	for i := range c {
		ids[i] = c[i].ID
	}
	return ctu.RemoveClubIDs(ids...)
}

// Save executes the query and returns the number of rows/vertices matched by this operation.
func (ctu *ClubTypeUpdate) Save(ctx context.Context) (int, error) {
	if v, ok := ctu.mutation.Name(); ok {
		if err := clubtype.NameValidator(v); err != nil {
			return 0, &ValidationError{Name: "name", err: fmt.Errorf("ent: validator failed for field \"name\": %w", err)}
		}
	}

	var (
		err      error
		affected int
	)
	if len(ctu.hooks) == 0 {
		affected, err = ctu.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*ClubTypeMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			ctu.mutation = mutation
			affected, err = ctu.sqlSave(ctx)
			mutation.done = true
			return affected, err
		})
		for i := len(ctu.hooks) - 1; i >= 0; i-- {
			mut = ctu.hooks[i](mut)
		}
		if _, err := mut.Mutate(ctx, ctu.mutation); err != nil {
			return 0, err
		}
	}
	return affected, err
}

// SaveX is like Save, but panics if an error occurs.
func (ctu *ClubTypeUpdate) SaveX(ctx context.Context) int {
	affected, err := ctu.Save(ctx)
	if err != nil {
		panic(err)
	}
	return affected
}

// Exec executes the query.
func (ctu *ClubTypeUpdate) Exec(ctx context.Context) error {
	_, err := ctu.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (ctu *ClubTypeUpdate) ExecX(ctx context.Context) {
	if err := ctu.Exec(ctx); err != nil {
		panic(err)
	}
}

func (ctu *ClubTypeUpdate) sqlSave(ctx context.Context) (n int, err error) {
	_spec := &sqlgraph.UpdateSpec{
		Node: &sqlgraph.NodeSpec{
			Table:   clubtype.Table,
			Columns: clubtype.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: clubtype.FieldID,
			},
		},
	}
	if ps := ctu.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if value, ok := ctu.mutation.Name(); ok {
		_spec.Fields.Set = append(_spec.Fields.Set, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: clubtype.FieldName,
		})
	}
	if nodes := ctu.mutation.RemovedClubIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.O2M,
			Inverse: false,
			Table:   clubtype.ClubTable,
			Columns: []string{clubtype.ClubColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeInt,
					Column: club.FieldID,
				},
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_spec.Edges.Clear = append(_spec.Edges.Clear, edge)
	}
	if nodes := ctu.mutation.ClubIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.O2M,
			Inverse: false,
			Table:   clubtype.ClubTable,
			Columns: []string{clubtype.ClubColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeInt,
					Column: club.FieldID,
				},
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_spec.Edges.Add = append(_spec.Edges.Add, edge)
	}
	if n, err = sqlgraph.UpdateNodes(ctx, ctu.driver, _spec); err != nil {
		if _, ok := err.(*sqlgraph.NotFoundError); ok {
			err = &NotFoundError{clubtype.Label}
		} else if cerr, ok := isSQLConstraintError(err); ok {
			err = cerr
		}
		return 0, err
	}
	return n, nil
}

// ClubTypeUpdateOne is the builder for updating a single ClubType entity.
type ClubTypeUpdateOne struct {
	config
	hooks    []Hook
	mutation *ClubTypeMutation
}

// SetName sets the name field.
func (ctuo *ClubTypeUpdateOne) SetName(s string) *ClubTypeUpdateOne {
	ctuo.mutation.SetName(s)
	return ctuo
}

// AddClubIDs adds the club edge to Club by ids.
func (ctuo *ClubTypeUpdateOne) AddClubIDs(ids ...int) *ClubTypeUpdateOne {
	ctuo.mutation.AddClubIDs(ids...)
	return ctuo
}

// AddClub adds the club edges to Club.
func (ctuo *ClubTypeUpdateOne) AddClub(c ...*Club) *ClubTypeUpdateOne {
	ids := make([]int, len(c))
	for i := range c {
		ids[i] = c[i].ID
	}
	return ctuo.AddClubIDs(ids...)
}

// Mutation returns the ClubTypeMutation object of the builder.
func (ctuo *ClubTypeUpdateOne) Mutation() *ClubTypeMutation {
	return ctuo.mutation
}

// RemoveClubIDs removes the club edge to Club by ids.
func (ctuo *ClubTypeUpdateOne) RemoveClubIDs(ids ...int) *ClubTypeUpdateOne {
	ctuo.mutation.RemoveClubIDs(ids...)
	return ctuo
}

// RemoveClub removes club edges to Club.
func (ctuo *ClubTypeUpdateOne) RemoveClub(c ...*Club) *ClubTypeUpdateOne {
	ids := make([]int, len(c))
	for i := range c {
		ids[i] = c[i].ID
	}
	return ctuo.RemoveClubIDs(ids...)
}

// Save executes the query and returns the updated entity.
func (ctuo *ClubTypeUpdateOne) Save(ctx context.Context) (*ClubType, error) {
	if v, ok := ctuo.mutation.Name(); ok {
		if err := clubtype.NameValidator(v); err != nil {
			return nil, &ValidationError{Name: "name", err: fmt.Errorf("ent: validator failed for field \"name\": %w", err)}
		}
	}

	var (
		err  error
		node *ClubType
	)
	if len(ctuo.hooks) == 0 {
		node, err = ctuo.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*ClubTypeMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			ctuo.mutation = mutation
			node, err = ctuo.sqlSave(ctx)
			mutation.done = true
			return node, err
		})
		for i := len(ctuo.hooks) - 1; i >= 0; i-- {
			mut = ctuo.hooks[i](mut)
		}
		if _, err := mut.Mutate(ctx, ctuo.mutation); err != nil {
			return nil, err
		}
	}
	return node, err
}

// SaveX is like Save, but panics if an error occurs.
func (ctuo *ClubTypeUpdateOne) SaveX(ctx context.Context) *ClubType {
	ct, err := ctuo.Save(ctx)
	if err != nil {
		panic(err)
	}
	return ct
}

// Exec executes the query on the entity.
func (ctuo *ClubTypeUpdateOne) Exec(ctx context.Context) error {
	_, err := ctuo.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (ctuo *ClubTypeUpdateOne) ExecX(ctx context.Context) {
	if err := ctuo.Exec(ctx); err != nil {
		panic(err)
	}
}

func (ctuo *ClubTypeUpdateOne) sqlSave(ctx context.Context) (ct *ClubType, err error) {
	_spec := &sqlgraph.UpdateSpec{
		Node: &sqlgraph.NodeSpec{
			Table:   clubtype.Table,
			Columns: clubtype.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: clubtype.FieldID,
			},
		},
	}
	id, ok := ctuo.mutation.ID()
	if !ok {
		return nil, &ValidationError{Name: "ID", err: fmt.Errorf("missing ClubType.ID for update")}
	}
	_spec.Node.ID.Value = id
	if value, ok := ctuo.mutation.Name(); ok {
		_spec.Fields.Set = append(_spec.Fields.Set, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: clubtype.FieldName,
		})
	}
	if nodes := ctuo.mutation.RemovedClubIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.O2M,
			Inverse: false,
			Table:   clubtype.ClubTable,
			Columns: []string{clubtype.ClubColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeInt,
					Column: club.FieldID,
				},
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_spec.Edges.Clear = append(_spec.Edges.Clear, edge)
	}
	if nodes := ctuo.mutation.ClubIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.O2M,
			Inverse: false,
			Table:   clubtype.ClubTable,
			Columns: []string{clubtype.ClubColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeInt,
					Column: club.FieldID,
				},
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_spec.Edges.Add = append(_spec.Edges.Add, edge)
	}
	ct = &ClubType{config: ctuo.config}
	_spec.Assign = ct.assignValues
	_spec.ScanValues = ct.scanValues()
	if err = sqlgraph.UpdateNode(ctx, ctuo.driver, _spec); err != nil {
		if _, ok := err.(*sqlgraph.NotFoundError); ok {
			err = &NotFoundError{clubtype.Label}
		} else if cerr, ok := isSQLConstraintError(err); ok {
			err = cerr
		}
		return nil, err
	}
	return ct, nil
}
